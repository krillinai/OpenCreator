{
  "name": "@opencreator/daemon",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "pnpm --filter @opencreator/protocol build && pnpm --filter @opencreator/config build && pnpm --filter @opencreator/skill-market build",
    "dev": "node scripts/dev.mjs",
    "prebuild": "pnpm --filter @opencreator/protocol build && pnpm --filter @opencreator/config build && pnpm --filter @opencreator/skill-market build",
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.json",
    "pretypecheck": "pnpm --filter @opencreator/protocol build && pnpm --filter @opencreator/config build && pnpm --filter @opencreator/skill-market build",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json",
    "pretest": "pnpm --filter @opencreator/protocol build && pnpm --filter @opencreator/config build && pnpm --filter @opencreator/skill-market build",
    "test": "vitest run --passWithNoTests",
    "templates:validate": "tsx --tsconfig tsconfig.scripts.json scripts/creator-presets.ts validate",
    "templates:compile": "tsx --tsconfig tsconfig.scripts.json scripts/creator-presets.ts compile",
    "generate:codex-protocol": "node scripts/generate-codex-app-server-protocol.mjs",
    "verify:schedule-upgrade": "node --import tsx scripts/verify-scheduled-task-upgrade.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@iarna/toml": "2.2.5",
    "@modelcontextprotocol/sdk": "1.29.0",
    "@opencreator/config": "workspace:*",
    "@opencreator/protocol": "workspace:*",
    "@opencreator/skill-market": "workspace:*",
    "better-sqlite3": "^12.11.1",
    "cron-parser": "^5.6.1",
    "cross-spawn": "^7.0.6",
    "fastify": "^5.2.1",
    "https-proxy-agent": "^7.0.6",
    "image-size": "2.0.2",
    "nanoid": "^5.0.9",
    "yaml": "^2.8.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/cross-spawn": "^6.0.6"
  }
}
