{
  "name": "@opencreator/daemon",
  "version": "0.1.0",
  "type": "module",
  "files": [
    "dist",
    "runtime"
  ],
  "scripts": {
    "predev": "pnpm --filter @opencreator/protocol build && pnpm --filter @opencreator/config build && pnpm --filter @opencreator/skill-market build && pnpm --filter @opencreator/writing-templates build",
    "dev": "node scripts/dev.mjs",
    "prebuild": "pnpm --filter @opencreator/protocol build && pnpm --filter @opencreator/config build && pnpm --filter @opencreator/skill-market build && pnpm --filter @opencreator/writing-templates build",
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.json",
    "pretypecheck": "pnpm --filter @opencreator/protocol build && pnpm --filter @opencreator/config build && pnpm --filter @opencreator/skill-market build && pnpm --filter @opencreator/writing-templates build",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json",
    "pretest": "pnpm --filter @opencreator/protocol build && pnpm --filter @opencreator/config build && pnpm --filter @opencreator/skill-market build && pnpm --filter @opencreator/writing-templates build",
    "test": "vitest run --passWithNoTests",
    "generate:codex-protocol": "node scripts/generate-codex-app-server-protocol.mjs",
    "verify:schedule-upgrade": "node --import tsx scripts/verify-scheduled-task-upgrade.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@iarna/toml": "2.2.5",
    "@modelcontextprotocol/sdk": "1.29.0",
    "@opencreator/config": "workspace:*",
    "@opencreator/protocol": "workspace:*",
    "@opencreator/skill-market": "workspace:*",
    "@opencreator/writing-templates": "workspace:*",
    "@remotion/renderer": "4.0.473",
    "better-sqlite3": "^12.11.1",
    "cheerio": "1.1.2",
    "cron-parser": "^5.6.1",
    "cross-spawn": "^7.0.6",
    "fastify": "^5.2.1",
    "https-proxy-agent": "^7.0.6",
    "marked": "^18.0.12",
    "nanoid": "^5.0.9",
    "pdf-parse": "1.1.1",
    "pdfkit": "^0.20.2",
    "sharp": "0.35.4",
    "toml": "4.2.0",
    "yaml": "^2.8.1",
    "yauzl": "3.4.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@remotion/bundler": "4.0.473",
    "@types/better-sqlite3": "^7.6.13",
    "@types/cross-spawn": "^6.0.6",
    "@types/pdf-parse": "1.1.5",
    "@types/pdfkit": "^0.17.6",
    "@types/yauzl": "3.4.0"
  }
}
